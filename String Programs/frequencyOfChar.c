#include<stdio.h>
#include<string.h>

void main(){
    char str[100];
    printf("enter a string = ");
    
    fgets(str,sizeof(str), stdin);
    printf("%s", str);
}

int Frequency(char *str){
    
}
